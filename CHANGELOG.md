# Changelog

## Added
- The docs site now shows its own penguin logo in the header, and browser tabs show a matching icon
- The home page opens with the logo next to the short introduction
- Every docs page is now available in German from the language menu in the header
- The top tab is now called Documentation, and the pages live under `/documentation/` instead of `/user-guide/`
