# Changelog

## Documentation

### Added
- A Control output page under Reference (English) that explains `output`, `max_output_messages`, and `max_output_bytes`, and walks through every root and button combination
- Each example on that page now shows a small picture of the Telegram chat next to its description; click a picture to see it full size

### Changed
- The configuration, button, menu, and functions pages now link to Control output, and the menu and functions pages no longer say that long results always arrive as several messages
- On the configuration page, the pointer to Control output is now a highlighted box at the end of the output section instead of a plain sentence
- The short documentation index now includes Control output, next to Configuration and CLI

### Fixed
- On Persian pages, the caption under an enlarged picture now uses the Persian font and reads from the right
